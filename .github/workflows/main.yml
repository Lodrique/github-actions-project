name: First workflow

on: push

jobs: 
  build:
    run-ons: ubuntu-latest

    steps:
     - uses: actions/checkout@v3

     - name: Run a sample github action
     - run: echo "this workflow runs successfully for theis particular action"
